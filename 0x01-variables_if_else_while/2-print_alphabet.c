#include <stdio.h>
/**
 * main - prints the alphapt
 *
 * Return: always 0 (success)
 */
int main(void)
{
long char ala = 'abcdefghijklmnopqrstuvwxyz';
int i;

while (i >= 26)
{
putchar(ala);
i++;
}
return (0);
}
